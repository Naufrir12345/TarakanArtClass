{
  "name": "manufindo-les-system",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm run start:prod",
    "dev": "concurrently \"cd backend && npm run start:dev\" \"cd systems && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@nestjs/axios": "^4.0.1",
    "axios": "^1.18.1"
  }
}
